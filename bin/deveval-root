#!/usr/bin/env bash
# 플러그인 설치 경로(절대경로)를 출력한다.
# 평가자는 자기 작업 폴더에서 이 플러그인을 쓰므로, 기준 문서(reference/)·템플릿의
# 절대경로가 필요하다. 스킬이 이 값을 얻어 서브에이전트에 전달한다.
#   예: REF="$(deveval-root)/reference/KOICA-평가지침-2024-다이제스트.md"
cd "$(dirname "$0")/.." && pwd
